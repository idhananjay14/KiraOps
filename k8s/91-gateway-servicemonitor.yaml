apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gateway
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - kiraops

  selector:
    matchLabels:
      app: gateway

  endpoints:
    - port: http
      path: /metrics
      interval: 15s
